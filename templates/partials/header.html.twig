<header>
    {% block nav %}
{{ dump(app) }}
{{ dump(app.user) }}
{{ dump(app.session) }}
{{ dump(app.request) }}

        {# NAVBAR #}
        <nav class="navbar navbar-expand-lg navbar-warning bg-warning">
            <a class="navbar-brand" href="{{ path('home') }}">
                <h1 class="beertime-title">Dev my shirt</h1>
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            {# <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <div class="navbar-nav">
                    <a class="nav-item nav-link" href="{{ path('list') }}">Liste des événements</a>
                    <a class="nav-item nav-link" href="{{ path('create') }}">Créer un événement</a>
                </div>
            </div> #}
            {# <form method="GET" action="{{ path('list') }}" class="form-inline my-2 my-lg-0">
                <input class="form-control mr-sm-2" type="text" placeholder="Evénement, ..." name="querySearch">
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Chercher</button>
            </form> #}
            {# <span>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</span> #}
            {# {% if app.user.connected %}
                <span>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</span>
                <span>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</span> #}
                {# Connecté #}
                {# <span>Bienvenue {{ app.user.username }} !</span> #}
            {# {% else %} #}
                {# S'inscrire #}
                <a class="btn btn-outline-success bg-success text-warning my-2 my-sm-0" href="{{ path('register') }}">S'inscrire</a>
                <span>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</span>
                {# Se connecter #}
                <a class="btn btn-outline-success bg-success text-warning my-2 my-sm-0" href="{{ path('login') }}">Se connecter</a>
            {# {% endif %} #}
        </nav>
    {% endblock %}
</header>